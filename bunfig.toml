[install]
# Next.js and parts of its ecosystem (e.g. @sentry/nextjs, which injects bare
# imports of transitive packages into app code) assume a classic flat
# node_modules. bun 1.3 defaults workspaces to the isolated linker, which
# breaks those assumptions, so pin the hoisted linker for the whole monorepo.
linker = "hoisted"
